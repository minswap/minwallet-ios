# MinWallet iOS Project

## Getting Started 🚀

### Setting up Rust Environment

1. Navigate to the rust directory:

```bash
cd rust
```

2. Build the project:

```bash
./build.sh
```

## Development Guide 💻

### Code Formatting

Keep your code clean and consistent by running:

```bash
swift format format -r -p -i MinWallet
```

### Code Linting

Check your code quality with:

```bash
swift format lint -r -p MinWallet
```

---

_Note: Make sure you have both Swift and Rust toolchains installed before proceeding._
